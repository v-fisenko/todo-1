<!-- maybe later will write: hide/show completed tasks
<%= link_to project_path(project), remote: true do %>
  <span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span>
<% end %> &nbsp;
--!>

<%= link_to 'javascript:void(0)', id: "edit_project_#{project.id}_btn" do %>
  <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
<% end %> &nbsp;

<!-- maybe later will write: move - set priority
<%= link_to 'javascript:void(0)', class: "handle" do %>
  <span class="glyphicon glyphicon-move" aria-hidden="true"></span>
<% end %> &nbsp;
--!>

<%= link_to project_path(project), method: :delete, remote: true, data: { confirm: "Are you sure?" }, id: "delete_project_#{project.id}_btn" do%>
  <span class="glyphicon glyphicon-trash"></span>
<% end %>
